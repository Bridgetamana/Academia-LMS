import SignUp from '@/app/components/Auth/SignUp';

export const metadata = {
  title: 'Sign Up',
  description: 'Create an Account',
};

const page = () => {
  return <SignUp />;
};

export default page;
